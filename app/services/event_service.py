from typing import List


async def list_events(user_id: str, start_date=None, end_date=None, page: int = 1, size: int = 20):
    # TODO: query DB and return events
    return []


async def create_event(user_id: str, payload):
    # TODO: create event in DB
    raise NotImplementedError
